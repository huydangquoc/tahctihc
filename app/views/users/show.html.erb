<table class="table">
  <thead class="thead thead-default">
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @new_friends.each do |nf| %>
      <tr>
        <td><%= nf.name %></td>
        <td><%= nf.email %></td>
        <%= link_to "Add Friend", contacts_add_path(:user_id => current_user.id, :friend_id => user.id) , class: "btn btn-danger"  %>
      </tr>
    <% end %>
  </tbody>
</table>